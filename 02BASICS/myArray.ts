const superHeroes: string[] = [];
const heroPower: Array<number> = [];

type User = {
    name: string,
    isActive: boolean,
}

const allUsers: User[] = [
    {
        name: "sample",
        isActive: false,
    }
];

const MLModels: number[][] = [
    [1, 2, 3],
    [1, 2, 4]
]

superHeroes.push("spiderman");
heroPower.push(2);

allUsers.push({
    name: "jc",
    isActive: true,
});